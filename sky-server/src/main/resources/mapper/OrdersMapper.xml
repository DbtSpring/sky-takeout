<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrdersMapper">
    <insert id="insert" parameterType="com.sky.entity.Orders" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders(number, status, user_id, address_book_id, order_time, checkout_time, pay_method, pay_status, amount, remark, phone, address, user_name, consignee, cancel_reason, rejection_reason, cancel_time, estimated_delivery_time, delivery_status, delivery_time, pack_amount, tableware_number, tableware_status)
        VALUES(#{number}, #{status}, #{userId}, #{addressBookId}, #{orderTime}, #{checkoutTime}, #{payMethod}, #{payStatus}, #{amount}, #{remark}, #{phone}, #{address}, #{userName}, #{consignee}, #{cancelReason}, #{rejectionReason}, #{cancelTime}, #{estimatedDeliveryTime}, #{deliveryStatus}, #{deliveryTime}, #{packAmount}, #{tablewareNumber}, #{tablewareStatus})
    </insert>

    <update id="updateByNumberAndUserId" parameterType="com.sky.entity.Orders">
        update orders
        <set>
            <if test="payStatus != null">
                pay_status = #{payStatus},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="checkoutTime != null">
                checkout_time = #{checkoutTime},
            </if>
        </set>
        where number = #{number} and user_id = #{userId}
    </update>

    <select id="ordersPageQuery" parameterType="com.sky.dto.OrdersPageQueryDTO" resultType="com.sky.entity.Orders">
        select * from orders
        <where>
            <if test="number != null">
                and number = #{number}
            </if>
            <if test="phone != null">
                and phone like concat('%',#{phone},'%')
            </if>
            <if test="beginTime != null">
                and order_time &gt;= #{beginTime}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="endTime != null">
                and order_time &lt;= #{endTime}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
        </where>
        order by order_time desc
    </select>

    <update id="updateById" parameterType="com.sky.entity.Orders">
        update orders
        <set>
            <if test="payStatus != null">
                pay_status = #{payStatus},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="cancelTime != null">
                cancel_time = #{cancelTime},
            </if>
            <if test="cancelReason != null">
                cancel_reason = #{cancelReason},
            </if>
            <if test="rejectionReason != null">
                rejection_reason = #{rejectionReason},
            </if>
        </set>
        where id=#{id}
    </update>

    <select id="getTurnoverByMap" parameterType="java.util.Map" resultType="java.lang.Double">
        select sum(amount) from orders
        #如果没有结果，count为0.但是为了防御性处理，还是返回Double而不是double

        <where>
            <if test="beginTime != null">
                and order_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and order_time &lt;= #{endTime}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <select id="getOrdersByMap" parameterType="java.util.Map" resultType="java.lang.Integer">
        select count(id) from orders
        <where>
            <if test="beginTime != null">
                and order_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and order_time &lt;= #{endTime}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

</mapper>