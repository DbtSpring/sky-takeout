<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">
    <insert id="insertBatch" parameterType="com.sky.entity.OrderDetail">
        INSERT INTO order_detail(name, image, order_id, dish_id, setmeal_id, dish_flavor, number, amount)
        VALUES
        <foreach collection="orderDetails" item="od" separator=",">
            (#{od.name}, #{od.image}, #{od.orderId}, #{od.dishId}, #{od.setmealId}, #{od.dishFlavor}, #{od.number}, #{od.amount})
        </foreach>
    </insert>

    <select id="getTop10ByMap" parameterType="java.util.Map" resultType="com.sky.dto.GoodsSalesDTO">
        select od.name as name , sum(od.number) as number from order_detail od
        inner join orders o
        on od.order_id = o.id
        <where>
            <if test="beginTime != null">
                and o.order_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and o.order_time &lt;= #{endTime}
            </if>
            <if test="status != null">
                and o.status = #{status}
            </if>
        </where>
        group by name
        order by number desc
        limit 0,10
    </select>

</mapper>